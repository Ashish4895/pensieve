# Browser calls nginx or Vite proxy; leave empty to use same-origin / relative /api
VITE_API_BASE_URL=
# WebSocket origin; leave empty to derive from window.location (nginx same-host)
# Dev direct-to-backend example: ws://127.0.0.1:8001
VITE_WS_BASE_URL=